import React from "react"
import { AppBar, Toolbar } from "@mui/material"

const Navigation = () => {
  return (
    <div>
        <AppBar>
              <Toolbar>
                  
            </Toolbar>
        </AppBar>
    </div>
  )
}

export default Navigation
